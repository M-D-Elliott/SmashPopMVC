@model SmashPopMVC.Models.Comment.CommentListingModel

@Html.Partial("../Comment/_NewPartial", Model.NewCommentModel)
<div id="comments-body">
    @foreach (var comment in Model.Comments)
    {
        @Html.Partial("../Comment/Edit", comment)
    }
</div>